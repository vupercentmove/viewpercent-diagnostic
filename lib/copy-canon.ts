/**
 * 카피 정본 — 금지어·성과 약속 규칙의 단일 출처.
 *
 * 정본: `docs/진단-언어-원칙-2026-07-25.md`. `question-examples.test.ts`와
 * `stage-examples.test.ts`가 각자 `BANNED` 배열을 복제해 들고 있다가
 * 성과 약속 검사가 한쪽에서만 빠지는 드리프트가 있었다(stage-examples.test.ts는
 * 금지어·느낌표만 검사하고 성과 약속은 검사하지 않았다). 두 테스트 모두 이 파일을
 * import해서 검사 목록이 다시 벌어지지 않게 한다. 규칙을 고칠 땐 여기 한 곳만 고친다.
 */

/** 자기채점 부사·단정 표현 금지어. "!"는 포함하지 않는다 — 느낌표는 호출부에서 별도 검사한다. */
export const BANNED_WORDS = [
  "충분히",
  "전략적으로",
  "적극적으로",
  "의도적으로",
  "무조건",
  "꼭",
  "무료",
  "당신",
] as const;

/** 성과 약속(성과를 보장하는 표현) 금지 정규식. */
export const PERFORMANCE_PROMISE_PATTERN = /더 팔|매출이 오|전환이 오|늘어납니다/;
