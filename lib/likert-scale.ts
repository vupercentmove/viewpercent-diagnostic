/**
 * 리커트 척도 표시 문구 — 세 진단 경로(빠른·심화·정밀)의 단일 출처.
 *
 * 1~5 숫자만 보여주면 어느 쪽이 좋은 상태인지 알 수 없다. `likertToScore` 기준으로
 * 1이 최저(0점), 5가 최고(100점)이므로 양 끝 앵커를 항상 함께 노출한다.
 *
 * 빠른·심화 진단에는 이 문구가 각각 하드코딩돼 있었고 정밀 진단에는 아예 빠져 있었다.
 * 세 경로가 어긋나지 않도록 여기로 모은다.
 */

/** 척도 왼쪽 끝(1점) 앵커 */
export const LIKERT_ANCHOR_LOW = "전혀 아님";

/** 척도 오른쪽 끝(5점) 앵커 */
export const LIKERT_ANCHOR_HIGH = "매우 그렇다";

/**
 * 스크린리더용 눈금 라벨.
 * "3점"만 읽히면 방향을 알 수 없으므로 양 끝에는 앵커 문구를 함께 읽어준다.
 * 중간값에 '보통' 같은 해석을 임의로 붙이지 않는다 — 화면에 없는 말을 만들지 않기 위해서다.
 */
export const LIKERT_OPTION_LABEL: Record<number, string> = {
  1: `1점 · ${LIKERT_ANCHOR_LOW}`,
  2: "2점",
  3: "3점",
  4: "4점",
  5: `5점 · ${LIKERT_ANCHOR_HIGH}`,
};
