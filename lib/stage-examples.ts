/**
 * 단계별 해석 예시 — 빠른 진단 결과 화면(PriorityCard, 1순위 개선점)의
 * 약점 단계 1개에만 쓴다.
 *
 * 문항 예시(question-examples.ts)가 "어디서 확인하는가"라면, 이쪽은
 * "이 점수면 보통 현장에서 어떤 모습인가"다.
 *
 * ⚠️ 6단계 전부에 뿌리지 않는다. 결과 화면이 이미 길고(빠른 진단 2,419자)
 * CTA 성격 버튼이 4개라, 가장 먼저 볼 구간 하나에만 붙인다.
 *
 * ⚠️ 정밀 진단 결과(FullResultLayout의 WeakestStageCard)에 다시 붙이지 않는다.
 * 그 카드는 이미 `FULL_DEEP_EXPLAINER`의 `ex.why`를 동일한
 * `text-[12.5px] text-gray-500` 스타일로 렌더하는데, 여기 예시가 단계마다
 * 그 문장을 바꿔 말할 뿐이었다 — STAGE 4는 `ex.why`가 "…장바구니까지 왔다가
 * 결제 직전에 마음을 접어요"로 끝나는데 예시는 "…장바구니까지는 오는데 결제
 * 직전에 마음을 접는 고객이 쌓여요"였고, STAGE 5는 "첫 경험이 밋밋하게"를
 * 그대로 반복했다. 대칭을 맞춘다고 다시 넣으면 이 중복이 재발한다.
 */
export const STAGE_EXAMPLE: Record<number, string> = {
  1: "이 구간이 얇으면 보통 이렇습니다 — 광고를 끄면 주문이 같이 멈추고, 검색이나 후기로 들어오는 길이 아직 좁아요.",
  2: "이 구간이 얇으면 보통 이렇습니다 — 들어온 고객이 메인만 보고 나가서, 상품 페이지까지 가는 사람이 적어요.",
  3: "이 구간이 얇으면 보통 이렇습니다 — 상품은 마음에 들어 하는데 '나한테 맞을까'가 안 풀려서 장바구니 앞에서 멈춰요.",
  4: "이 구간이 얇으면 보통 이렇습니다 — 장바구니까지는 오는데 결제 직전에 마음을 접는 고객이 쌓여요.",
  5: "이 구간이 얇으면 보통 이렇습니다 — 주문 뒤 기다리는 동안 연락이 없어서, 첫 경험이 밋밋하게 끝나요.",
  6: "이 구간이 얇으면 보통 이렇습니다 — 한 번 산 고객이 다시 오지 않아서, 매달 새 고객을 처음부터 설득하게 돼요.",
};

/** 예시가 없으면 빈 문자열 — 호출부는 빈 문자열이면 렌더하지 않는다. */
export function getStageExample(stageId: number): string {
  return STAGE_EXAMPLE[stageId] ?? "";
}
