# HANDOFF 2026-08-12 — viewpercent-diagnostic

작성: 맥미니 Claude Code 세션. 저장소 상태: main = origin/main (a8d17a5 기준), 워킹트리 깨끗,
테스트 190개 통과, 프로덕션 https://diagnostic.vupercent.com 200.

⚠️ 이 문서는 **AI 코멘트·심화 카드 계열 작업**만 다룬다. 같은 날 다른 세션이
`feat/full-deep-mode`(진단 OS 지표 정직화 — 유입경로 카드, ctaRate 정직화,
wiring-test 행 처리)를 병합했다(de69600). 그쪽 배경은 해당 커밋들과
`app/api/admin/stats/route.ts` 주석을 볼 것. 병합 후 테스트 190개·타입체크·
프로덕션 200을 이 세션에서 재확인했다.

## 오늘 한 일 (전부 배포·실경로 검증 완료)

시작은 "심화 결과 카드 정비"였고, AI 코멘트 톤 점검으로 이어지며 후속 5건까지 닫았다.

| 커밋 | 내용 |
|---|---|
| 3582266 | 심화 결과 카드 — 공통 점수 체계(getTag) 통일, 약점별 근거 문항 표시, 자동 스크롤 |
| 9acf70a | AI 코멘트 톤 — 폴백 부정 이미지 연속 완화, 프롬프트에 느낌표·'무조건/꼭'·부정 연속 금지 |
| 9c73ada | 프롬프트에 수치 날조 금지 규칙 (프로덕션에서 "이탈률 20%" 날조 확인 후) |
| 9bd216e | CLAUDE.md 톤 가드 문서화 + `scripts/wait-for-vercel-deploy.sh` 신설 |
| fd1016f | admin/stats 주석 정정 — RPC는 이미 full 모드를 제외하고 있었다 |
| 876bb7a (PR #19) | 심화 완료 시 이중 smooth 스크롤 경합 제거 (별도 세션이 브라우저 실측 검증) |
| 3059e21 | 수치 가드(결정적 후처리) + 폴백률 집계 + quick 정적 폴백 |
| 272445a | 단계명 정본 통일 + quick 폴백 회귀 테스트 |
| 7d6f316 | CLAUDE.md — 단계명 정본 규칙 추가, quick 503 설명 3곳 현행화 |

## 알아둘 것

- **단계명 정본은 `lib/stage-meta.ts`의 STAGES.** analyze 라우트·어드민이 각자 복제 표를
  들고 STAGE 2·3·4를 다르게(3을 "구매결정"으로) 표시해, AI 프롬프트가 화면과 다른 단계명을
  알려주고 있었다. 회귀 테스트(`app/api/analyze/route.test.ts`)를 쓰다 발견. 표를 다시 만들지 말 것.
- **AI 코멘트 방어는 2중이다.** 프롬프트 규칙(확률적) + `lib/numeric-guard.ts`(결정적).
  퍼센트·배수 중 실측 점수에 없는 값이 나오면 정적 폴백으로 교체하고 reason을 남긴다.
- **통계 집계 로직은 레포에 없다.** Supabase 프로젝트 `vupercent`(icqvvbxcaqrwykuhsfwb)의
  `get_diagnostic_stats()` / `get_ai_comment_stats()`. 통계가 이상하면 레포보다 함수 정의를 먼저 조회.
- **배포 검증은 `./scripts/wait-for-vercel-deploy.sh`** 한 번이면 된다 (Ready → 도메인 alias → 실경로 200).
  Vercel Ready만으로는 커스텀 도메인 반영이 보장되지 않는다.

## 미검증 / 남은 것

- **어드민 "AI 폴백률" 타일은 브라우저 실화면 미검증.** 어드민이 비밀번호 인증이라 이 세션에서
  화면을 열지 못했다. 데이터 경로(테이블 insert → RPC 집계)는 프로덕션에서 확인했고 빌드·타입도
  통과. 다음에 어드민에 로그인할 때 타일 표시만 눈으로 확인하면 된다.
- `ai_comment_events`는 검증용 행 3건을 지워 현재 0건. 실사용자 데이터가 쌓이기 시작하는 상태.
- 후속 제안 중 미착수: 없음 (이 세션이 낸 1~5번은 전부 처리).
- 어드민 대시보드는 오늘 두 세션이 각각 손댔다(이쪽 "AI 폴백률" 타일 / 저쪽 유입경로·ctaRate).
  다음에 어드민을 열면 두 변경이 한 화면에서 어떻게 보이는지 같이 확인하면 좋다.
